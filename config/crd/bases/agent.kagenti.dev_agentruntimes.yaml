---
apiVersion: apiextensions.k8s.io/v1
kind: CustomResourceDefinition
metadata:
  annotations:
    controller-gen.kubebuilder.io/version: v0.20.1
  name: agentruntimes.agent.kagenti.dev
spec:
  group: agent.kagenti.dev
  names:
    kind: AgentRuntime
    listKind: AgentRuntimeList
    plural: agentruntimes
    singular: agentruntime
  scope: Namespaced
  versions:
  - additionalPrinterColumns:
    - jsonPath: .spec.type
      name: Type
      type: string
    - jsonPath: .status.phase
      name: Phase
      type: string
    - jsonPath: .status.gateway.gatewayEndpoint
      name: Endpoint
      priority: 1
      type: string
    - jsonPath: .metadata.creationTimestamp
      name: Age
      type: date
    name: v1alpha1
    schema:
      openAPIV3Schema:
        description: AgentRuntime is the Schema for the agentruntimes API.
        properties:
          apiVersion:
            description: |-
              APIVersion defines the versioned schema of this representation of an object.
              Servers should convert recognized schemas to the latest internal value, and
              may reject unrecognized values.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#resources
            type: string
          kind:
            description: |-
              Kind is a string value representing the REST resource this object represents.
              Servers may infer this from the endpoint the client submits requests to.
              Cannot be updated.
              In CamelCase.
              More info: https://git.k8s.io/community/contributors/devel/sig-architecture/api-conventions.md#types-kinds
            type: string
          metadata:
            type: object
          spec:
            description: AgentRuntimeSpec defines the desired state of AgentRuntime.
            properties:
              identity:
                description: IdentityConfig configures workload identity.
                properties:
                  spiffe:
                    description: SPIFFEConfig configures SPIFFE-based identity.
                    properties:
                      trustDomain:
                        type: string
                    required:
                    - trustDomain
                    type: object
                type: object
              policy:
                description: PolicyConfig defines network access control and authorization
                  policy.
                properties:
                  allowedIngressNamespaces:
                    items:
                      type: string
                    type: array
                  dependencies:
                    items:
                      description: Dependency declares a target this agent is allowed
                        to call.
                      properties:
                        allowedSkills:
                          items:
                            type: string
                          type: array
                        allowedTools:
                          items:
                            type: string
                          type: array
                        name:
                          type: string
                        namespace:
                          type: string
                      required:
                      - name
                      type: object
                    type: array
                  externalEgress:
                    items:
                      description: ExternalEgressRule allows egress to an external
                        endpoint.
                      properties:
                        cidr:
                          type: string
                        host:
                          type: string
                        port:
                          format: int32
                          type: integer
                      required:
                      - port
                      type: object
                    type: array
                type: object
              targetRef:
                description: TargetRef identifies the workload this AgentRuntime manages.
                properties:
                  apiVersion:
                    default: apps/v1
                    type: string
                  kind:
                    enum:
                    - Deployment
                    - StatefulSet
                    - Sandbox
                    type: string
                  name:
                    type: string
                required:
                - apiVersion
                - kind
                - name
                type: object
              trace:
                description: TraceConfig configures OpenTelemetry tracing.
                properties:
                  endpoint:
                    type: string
                  protocol:
                    default: grpc
                    enum:
                    - grpc
                    - http
                    type: string
                  samplingRate:
                    format: int32
                    type: integer
                required:
                - endpoint
                type: object
              type:
                default: agent
                enum:
                - agent
                - tool
                type: string
            required:
            - targetRef
            - type
            type: object
          status:
            description: AgentRuntimeStatus defines the observed state of AgentRuntime.
            properties:
              card:
                description: CardStatus caches the agent's A2A card fetched on rollout.
                properties:
                  fetchedAt:
                    format: date-time
                    type: string
                  fetchedOverMTLS:
                    type: boolean
                  name:
                    type: string
                  skills:
                    items:
                      type: string
                    type: array
                  version:
                    type: string
                type: object
              conditions:
                items:
                  description: Condition contains details for one aspect of the current
                    state of this API Resource.
                  properties:
                    lastTransitionTime:
                      description: |-
                        lastTransitionTime is the last time the condition transitioned from one status to another.
                        This should be when the underlying condition changed.  If that is not known, then using the time when the API field changed is acceptable.
                      format: date-time
                      type: string
                    message:
                      description: |-
                        message is a human readable message indicating details about the transition.
                        This may be an empty string.
                      maxLength: 32768
                      type: string
                    observedGeneration:
                      description: |-
                        observedGeneration represents the .metadata.generation that the condition was set based upon.
                        For instance, if .metadata.generation is currently 12, but the .status.conditions[x].observedGeneration is 9, the condition is out of date
                        with respect to the current state of the instance.
                      format: int64
                      minimum: 0
                      type: integer
                    reason:
                      description: |-
                        reason contains a programmatic identifier indicating the reason for the condition's last transition.
                        Producers of specific condition types may define expected values and meanings for this field,
                        and whether the values are considered a guaranteed API.
                        The value should be a CamelCase string.
                        This field may not be empty.
                      maxLength: 1024
                      minLength: 1
                      pattern: ^[A-Za-z]([A-Za-z0-9_,:]*[A-Za-z0-9_])?$
                      type: string
                    status:
                      description: status of the condition, one of True, False, Unknown.
                      enum:
                      - "True"
                      - "False"
                      - Unknown
                      type: string
                    type:
                      description: type of condition in CamelCase or in foo.example.com/CamelCase.
                      maxLength: 316
                      pattern: ^([a-z0-9]([-a-z0-9]*[a-z0-9])?(\.[a-z0-9]([-a-z0-9]*[a-z0-9])?)*/)?(([A-Za-z0-9][-A-Za-z0-9_.]*)?[A-Za-z0-9])$
                      type: string
                  required:
                  - lastTransitionTime
                  - message
                  - reason
                  - status
                  - type
                  type: object
                type: array
                x-kubernetes-list-map-keys:
                - type
                x-kubernetes-list-type: map
              configuredPods:
                format: int32
                type: integer
              gateway:
                description: GatewayStatus reports the state of generated gateway
                  resources.
                properties:
                  authPolicyName:
                    type: string
                  gatewayEndpoint:
                    type: string
                  httpRouteName:
                    type: string
                  networkPolicyName:
                    type: string
                type: object
              identity:
                description: IdentityStatus reports the agent's identity state.
                properties:
                  certificateSource:
                    type: string
                  mtlsEnabled:
                    type: boolean
                  spiffeId:
                    type: string
                type: object
              observedGeneration:
                format: int64
                type: integer
              phase:
                default: Pending
                enum:
                - Pending
                - Active
                - Error
                type: string
            type: object
        required:
        - spec
        type: object
    served: true
    storage: true
    subresources:
      status: {}
