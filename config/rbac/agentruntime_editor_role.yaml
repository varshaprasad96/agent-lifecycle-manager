# This rule is not used by the project agent-lifecycle-manager itself.
# It is provided to allow the cluster admin to help manage permissions for users.
#
# Grants permissions to create, update, and delete resources within the agent.kagenti.dev.
# This role is intended for users who need to manage these resources
# but should not control RBAC or manage permissions for others.

apiVersion: rbac.authorization.k8s.io/v1
kind: ClusterRole
metadata:
  labels:
    app.kubernetes.io/name: agent-lifecycle-manager
    app.kubernetes.io/managed-by: kustomize
  name: agentruntime-editor-role
rules:
- apiGroups:
  - agent.kagenti.dev
  resources:
  - agentruntimes
  verbs:
  - create
  - delete
  - get
  - list
  - patch
  - update
  - watch
- apiGroups:
  - agent.kagenti.dev
  resources:
  - agentruntimes/status
  verbs:
  - get
